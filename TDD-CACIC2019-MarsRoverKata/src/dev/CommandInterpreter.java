package dev;

public interface CommandInterpreter {
	/**Process one command for a MarsRover instance.
	 * Procesa un comando para una instancia de MarsRover.   
	 */
	void processCommand(MarsRover rover);
}
